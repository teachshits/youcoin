<%= form_tag(  { :action => :change_balance, :id => @cash }, :remote => true )  do %>

Новое значение баланса:
    <%= text_field_tag :new_balance, (number_with_precision(@cash.balance, :precision => 2) || 0) %>
    
    
<div class="actions">
    <%= submit_tag "Изменить" %>
  </div>
  
<% end %>